# Liquidity Heatmap live metric cards fix

Fixed:
- Market Cap now uses liveMarketCap
- Volume now uses liveVolume
- Dominance now uses liveDominance
- Metric cards now animate subtly with live pulse

This keeps the existing UI and only makes the displayed metrics visibly realtime.
