import { canonicalChecksum } from "@/lib/data-platform/contracts"
import type { AvailableTemporalAlignmentInput, TemporalAlignmentInput, TemporalAlignmentRequest } from "./temporalContracts"
function reference(input:TemporalAlignmentInput){return input.availability==="AVAILABLE"?{availability:input.availability,roleId:input.roleId,canonicalRecordId:input.fact.canonicalRecordId,recordVersion:input.fact.recordVersion,checksum:input.checksum}:{availability:input.availability,roleId:input.roleId,datasetId:input.datasetId,reasonCode:input.reasonCode}}
export function temporalAlignmentIdentity(request:TemporalAlignmentRequest):string { const refs=request.inputs.map(reference).sort((a,b)=>canonicalChecksum(a).localeCompare(canonicalChecksum(b))); return "talign_"+canonicalChecksum({mode:request.policy.mode,policy:request.policy,eventTimeWindow:request.eventTimeWindow,knowledgeTime:request.knowledgeTime,inputs:refs,runId:request.runSpecification.runId,runSpecificationChecksum:request.runSpecification.specificationChecksum}) }
export function temporalOutcomeChecksum(material:Omit<import("./temporalContracts").TemporalAlignmentOutcome,"checksum"|"createdAt">):string{return canonicalChecksum(material)}
