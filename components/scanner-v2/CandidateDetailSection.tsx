import React from "react"
import { AvailabilityBadge } from "@/components/feedback"
import type { InvestigationCandidateViewModel } from "@/lib/scanner-presentation/contracts"

export function CandidateDetailSection({ model }: { readonly model: InvestigationCandidateViewModel | null }) {
  return <section aria-labelledby="candidate-detail-title" className="grid gap-4"><div><p className="text-xs font-semibold uppercase text-[var(--qt-color-evidence)]">Bounded summary composition</p><h2 id="candidate-detail-title" className="mt-1 text-lg font-semibold">Candidate Detail</h2></div>{model ? <article className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="text-base font-semibold">{model.symbol}</h3><AvailabilityBadge availability={model.identity.availability} /></div><p className="text-sm">Detected pattern: {model.detectedPattern ?? "UNAVAILABLE"}</p><p className="text-sm">Source-model direction: {model.sourceDirection ?? "UNAVAILABLE"}</p><p className="text-sm">Source-model explanation: {model.sourceExplanation ?? "UNAVAILABLE"}</p><p className="text-xs text-[var(--qt-color-warning)]">{model.identity.limitation}</p><ul className="grid gap-1 text-xs text-[var(--qt-color-text-muted)]">{model.limitations.map((item) => <li key={item}>{item}</li>)}</ul></article> : <p className="text-sm text-[var(--qt-color-text-muted)]">Candidate detail is unavailable because no candidate was supplied.</p>}</section>
}
