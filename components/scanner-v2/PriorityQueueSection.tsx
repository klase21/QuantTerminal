import React from "react"
import { AvailabilityBadge, FreshnessIndicator } from "@/components/feedback"
import type { InvestigationQueueViewModel } from "@/lib/scanner-presentation/contracts"

export function PriorityQueueSection({ model }: { readonly model: InvestigationQueueViewModel }) {
  return <section aria-labelledby="scanner-queue-title" className="grid gap-4"><div><p className="text-xs font-semibold uppercase text-[var(--qt-color-evidence)]">Priority for investigation</p><h2 id="scanner-queue-title" className="mt-1 text-lg font-semibold">Investigation Queue</h2><p className="mt-1 text-xs text-[var(--qt-color-text-muted)]">{model.orderingBasis}</p></div><div role="list" className="grid gap-3">{model.candidates.map((candidate, index) => <article role="listitem" key={`${candidate.symbol}-${index}`} className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs text-[var(--qt-color-text-muted)]">Queue position {index + 1}; presentation position is not identity</p><h3 className="text-base font-semibold">{candidate.symbol}</h3></div><AvailabilityBadge availability={candidate.availability} /></div><div className="grid gap-2 sm:grid-cols-3"><p className="text-sm">Score: {candidate.priority.score ?? "UNAVAILABLE"}</p><p className="text-sm">Tier: {candidate.priority.tier ?? "UNAVAILABLE"}</p><p className="text-sm">Confidence: UNAVAILABLE</p></div><p className="text-xs font-semibold text-[var(--qt-color-warning)]">{candidate.priority.label}</p><p className="text-xs text-[var(--qt-color-text-secondary)]">{candidate.priority.limitation}</p><FreshnessIndicator freshness={candidate.freshness} />{candidate.retentionState ? <p className="text-xs">Retention state: {candidate.retentionState}. This is not source freshness.</p> : null}</article>)}</div></section>
}
