import React from "react"
import { AvailabilityBadge } from "@/components/feedback"
import type { MarketsInvestigationHandoffViewModel, SecondaryMoverViewModel } from "@/lib/markets-presentation/contracts"

export function MarketsHandoffs({ movers, scanner, onSelectSymbol, onOpenScanner }: { readonly movers: readonly SecondaryMoverViewModel[]; readonly scanner: MarketsInvestigationHandoffViewModel; readonly onSelectSymbol: (symbol: string) => void; readonly onOpenScanner: () => void }) { return <section aria-labelledby="markets-handoffs-title" className="grid gap-4"><div><p className="text-xs font-semibold uppercase text-[var(--qt-color-evidence)]">Investigation Handoffs</p><h2 id="markets-handoffs-title" className="mt-1 text-lg font-semibold">Secondary Discovery Context</h2></div>{movers.length ? <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{movers.map((mover) => <article key={mover.id} className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><div className="flex flex-wrap items-center justify-between gap-2"><button type="button" onClick={() => onSelectSymbol(mover.symbol)} className="min-h-[var(--qt-touch-target)] rounded-[var(--qt-radius-control)] text-left text-sm font-semibold underline-offset-4 hover:underline">{mover.symbol}</button><AvailabilityBadge availability={mover.availability} /></div><p className="text-sm">Observed change: {mover.observedPriceChangePercent ?? "UNAVAILABLE"}%</p><p className="text-sm">Observed quote volume: {mover.observedQuoteVolume ?? "UNAVAILABLE"}</p><p className="text-xs text-[var(--qt-color-text-muted)]">Source classification: {mover.sourceClassification ?? "UNAVAILABLE"}; source action: {mover.sourceAction ?? "UNAVAILABLE"}</p><p className="text-xs text-[var(--qt-color-warning)]">{mover.limitation}</p></article>)}</div> : <p className="text-sm text-[var(--qt-color-text-muted)]">Secondary mover context is unavailable.</p>}<article className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><h3 className="text-sm font-semibold">Scanner handoff</h3><p className="text-sm text-[var(--qt-color-text-secondary)]">{scanner.description}</p><button type="button" disabled={!scanner.available} onClick={onOpenScanner} className="min-h-[var(--qt-touch-target)] justify-self-start rounded-[var(--qt-radius-control)] border border-[var(--qt-color-border)] px-3 text-sm font-semibold">{scanner.label}</button>{scanner.limitation ? <p className="text-xs text-[var(--qt-color-warning)]">{scanner.limitation}</p> : null}</article></section> }
