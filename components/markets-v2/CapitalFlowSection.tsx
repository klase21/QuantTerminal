import React from "react"
import { AvailabilityBadge, FreshnessIndicator, ProvenanceLabel, StatePanel } from "@/components/feedback"
import type { CapitalFlowViewModel } from "@/lib/markets-presentation/contracts"

function value(value: number | null, unit: string | null) { return value === null ? "UNAVAILABLE" : `${value.toLocaleString()}${unit ? ` ${unit}` : ""}` }
export function CapitalFlowSection({ model }: { readonly model: CapitalFlowViewModel }) {
  return <section aria-labelledby="capital-flow-title" className="grid gap-4"><div><p className="text-xs font-semibold uppercase text-[var(--qt-color-evidence)]">Capital Flow</p><h2 id="capital-flow-title" className="mt-1 text-lg font-semibold">Flow contracts and separate reserve evidence</h2></div><div className="grid gap-3 lg:grid-cols-2"><article className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="text-sm font-semibold">ETF Flow</h3><AvailabilityBadge availability={model.etf.availability} /></div>{model.etf.value !== null ? <><p className="text-xl font-semibold">{value(model.etf.value, model.etf.unit)}</p><p className="text-xs text-[var(--qt-color-text-muted)]">{model.etf.asset} / source date {model.etf.sourceDate ?? "UNAVAILABLE"}</p></> : <StatePanel state={model.etf.lifecycle} title="ETF Flow" reason={model.etf.availability.reason} />}<FreshnessIndicator freshness={model.etf.freshness} />{model.etf.provenance ? <ProvenanceLabel provenance={model.etf.provenance} /> : null}<p className="text-xs text-[var(--qt-color-warning)]">{model.etf.limitation}</p></article><article className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="text-sm font-semibold">Reserve Evidence</h3><AvailabilityBadge availability={model.reserve.availability} /></div>{model.reserve.asset ? <div className="grid gap-2 text-sm"><p>Asset: {model.reserve.asset}</p><p>Balance: {model.reserve.balance ?? "UNAVAILABLE"}</p><p>Balance USD: {model.reserve.balanceUsd ?? "UNAVAILABLE"}</p><p>Balance change: {model.reserve.balanceChange ?? "UNAVAILABLE"}</p><p>Balance USD change: {model.reserve.balanceUsdChange ?? "UNAVAILABLE"}</p></div> : <StatePanel state={model.reserve.lifecycle} title="Reserve Evidence" reason={model.reserve.availability.reason} />}<FreshnessIndicator freshness={model.reserve.freshness} /><p className="text-xs text-[var(--qt-color-warning)]">{model.reserve.limitation}</p></article></div><div className="grid gap-3 md:grid-cols-3">{model.categories.map((category) => <StatePanel key={category.id} state={category.lifecycle} title={`${category.label} UNAVAILABLE`} reason={category.limitation} />)}</div></section>
}
