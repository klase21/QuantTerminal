import React from "react"
import Link from "next/link"
import { StatePanel } from "@/components/feedback"
import type { DecisionHandoffViewModel, DecisionUnavailableViewModel, TradeRepositoryViewModel } from "@/lib/trade-presentation/contracts"

export function DecisionHandoffs({ monitoring, handoffs, repository }: { readonly monitoring: DecisionUnavailableViewModel; readonly handoffs: readonly DecisionHandoffViewModel[]; readonly repository: TradeRepositoryViewModel }) { return <section aria-labelledby="decision-handoffs-title" className="grid gap-4"><h2 id="decision-handoffs-title" className="text-lg font-semibold">Monitoring, Review, and Investigation Handoffs</h2><StatePanel state={monitoring.lifecycle} title="Canonical Monitoring and Review UNAVAILABLE" reason={monitoring.reason} /><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-5">{handoffs.map((item) => <article key={item.id} className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><h3 className="text-sm font-semibold">{item.id}</h3><Link href={item.href} className="min-h-[var(--qt-touch-target)] rounded-[var(--qt-radius-control)] border border-[var(--qt-color-border)] px-3 py-3 text-sm font-semibold">{item.label}</Link><p className="text-xs text-[var(--qt-color-warning)]">{item.limitation}</p></article>)}</div><StatePanel state={repository.lifecycle} title="Repository Handoff UNAVAILABLE" reason={repository.reason} /></section> }
