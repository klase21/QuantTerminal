import React from "react"
import { AvailabilityBadge } from "@/components/feedback"
import type { DecisionRiskViewModel } from "@/lib/trade-presentation/contracts"

export function RiskAssessmentSection({ model }: { readonly model: DecisionRiskViewModel }) { const groups = [["Source-model risk", model.sourceModelRisk], ["Local heuristic risk", model.localHeuristicRisk], ["User-supplied risk", model.userRisk], ["Missing risk inputs", model.missingRisk]] as const; return <section aria-labelledby="risk-assessment-title" className="grid gap-4"><div className="flex flex-wrap items-center justify-between gap-2"><h2 id="risk-assessment-title" className="text-lg font-semibold">Risk Assessment</h2><AvailabilityBadge availability={model.availability} /></div><div className="grid gap-3 md:grid-cols-2">{groups.map(([label, items]) => <article key={label} className="grid gap-2 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><h3 className="text-sm font-semibold">{label}</h3>{items.length ? <ul className="grid gap-1 text-sm text-[var(--qt-color-text-secondary)]">{items.map((item) => <li key={item}>{item}</li>)}</ul> : <p className="text-sm text-[var(--qt-color-text-muted)]">UNAVAILABLE</p>}</article>)}</div><p className="text-xs text-[var(--qt-color-warning)]">{model.limitation}</p></section> }
