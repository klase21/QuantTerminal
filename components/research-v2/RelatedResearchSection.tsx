import React from "react"
import { AvailabilityBadge, FreshnessIndicator, StatePanel } from "@/components/feedback"
import type { RelatedResearchViewModel } from "@/lib/research-presentation/contracts"

export interface RelatedResearchActions { readonly onSelectHistoricalCase: (id: string) => void; readonly onLoadHistorical: () => void; readonly onLoadEventImpact: () => void; readonly onLoadMarketMemory: () => void; readonly historicalLoading: boolean; readonly eventImpactLoading: boolean; readonly marketMemoryLoading: boolean }
export function RelatedResearchSection({ items, actions }: { readonly items: readonly RelatedResearchViewModel[]; readonly actions: RelatedResearchActions }) {
  return <section aria-labelledby="related-research-title" className="grid gap-4"><div><p className="text-xs font-semibold uppercase text-[var(--qt-color-evidence)]">Related Research</p><h2 id="related-research-title" className="mt-1 text-lg font-semibold">Manual historical investigations</h2></div><div className="flex flex-wrap gap-2"><button type="button" onClick={actions.onLoadHistorical} disabled={actions.historicalLoading} className="min-h-[var(--qt-touch-target)] rounded-[var(--qt-radius-control)] border border-[var(--qt-color-border)] px-3 text-xs font-semibold">{actions.historicalLoading ? "Loading Historical Analog" : "Load Historical Analog"}</button><button type="button" onClick={actions.onLoadEventImpact} disabled={actions.eventImpactLoading} className="min-h-[var(--qt-touch-target)] rounded-[var(--qt-radius-control)] border border-[var(--qt-color-border)] px-3 text-xs font-semibold">{actions.eventImpactLoading ? "Loading Event Impact" : "Load Event Impact"}</button><button type="button" onClick={actions.onLoadMarketMemory} disabled={actions.marketMemoryLoading} className="min-h-[var(--qt-touch-target)] rounded-[var(--qt-radius-control)] border border-[var(--qt-color-border)] px-3 text-xs font-semibold">{actions.marketMemoryLoading ? "Loading Market Memory" : "Load Market Memory"}</button></div>{items.length ? <div className="grid gap-3 lg:grid-cols-2">{items.map((item) => <article key={item.id} aria-current={item.selected ? "true" : undefined} className="grid gap-3 rounded-[var(--qt-radius-card)] border border-[var(--qt-color-border)] bg-[var(--qt-color-surface)] p-[var(--qt-space-4)]"><div className="flex flex-wrap items-start justify-between gap-2"><div><p className="text-xs text-[var(--qt-color-text-muted)]">{item.kind.replaceAll("_", " ")}</p><h3 className="break-words text-sm font-semibold">{item.title}</h3></div><AvailabilityBadge availability={item.availability} /></div><p className="text-sm text-[var(--qt-color-text-secondary)]">{item.summary}</p><FreshnessIndicator freshness={item.freshness} />{item.kind === "HISTORICAL_ANALOG" ? <button type="button" aria-pressed={item.selected} onClick={() => actions.onSelectHistoricalCase(item.id)} className="min-h-[var(--qt-touch-target)] justify-self-start rounded-[var(--qt-radius-control)] border border-[var(--qt-color-border)] px-3 text-xs font-semibold">{item.selected ? "Selected case" : "Select case"}</button> : null}</article>)}</div> : <StatePanel state="EMPTY" title="Related Research" reason="Historical sources remain manual-load workflows." />}</section>
}
